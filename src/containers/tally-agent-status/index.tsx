"use client";
/**
 *
 * TallyAgentStatusContainer
 *
 */

import { FC } from "react";

const TallyAgentStatusContainer: FC = () => {
	const getContent = () => {
		return (
			<>
				<h2 className="mb-4 text-xl font-semibold text-gray-800">Tally Agent Status</h2>
			</>
		);
	};

	return getContent();
};

export default TallyAgentStatusContainer;
