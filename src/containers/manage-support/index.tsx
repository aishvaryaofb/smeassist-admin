"use client";
/**
 *
 * ManageSupportContainer
 *
 */

import { FC } from "react";

const ManageSupportContainer: FC = () => {
	const getContent = () => {
		return (
			<>
				<h2 className="mb-4 text-xl font-semibold text-gray-800">Manage Support</h2>
			</>
		);
	};

	return getContent();
};

export default ManageSupportContainer;
